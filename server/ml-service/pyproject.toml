[tool.poetry]
name = "bovisionai-ml"
version = "0.1.0"
description = ""
authors = ["you"]

[tool.poetry.dependencies]
python = ">=3.10,<3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
